<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.segwangYouth.attendanceMapper">

    <select id="selectMemberList" resultType="java.util.Map">
	SELECT MEMBER_NAME
		 , (SELECT CODE_NAME FROM CODE_DETAIL WHERE HIGHER_CLASS_CODE ='AC' AND CODE_NO = VILLAGE_CODE) AS VILLAGE_CODE
		 , (SELECT CODE_NAME FROM CODE_DETAIL WHERE HIGHER_CLASS_CODE ='AA' AND CODE_NO = MEMBER_POSITION) AS MEMBER_POSITION
	 FROM  MEMBER
	 ORDER BY VILLAGE_CODE DESC
    </select>
    
</mapper>